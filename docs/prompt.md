I am setting up a custom Docker sandbox image called `openclaw-lmstudio`. The image runs OpenClaw (an AI coding agent) inside a Docker Sandbox, using LM Studio as the local model backend via a TypeScript bridge. Inspired by information in this article - https://www.docker.com/blog/run-openclaw-securely-in-docker-sandboxes/

Three main tasks:
1 - Please help me vuild this and deploy loclly using docker sandbox. 
2 - Migrate existing openclase workspace to sandbox or map directory into docker container.
3 - Create an easy to follow github irepo so people can create their own. 
